<UserControl x:Class="arches_arcgispro_addin.MainDockpaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="clr-namespace:arches_arcgispro_addin"
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="340"
             d:DataContext="{Binding Path=ui.MainDockpaneViewModel}">
    <Grid>
        <!-- Setup panel: shown when arches_config.json is not configured -->
        <StackPanel Name="SetupPanel" Margin="20,20,20,20" Visibility="Collapsed">
            <TextBlock Text="Configure Arches Connection" FontWeight="SemiBold" Margin="0,0,0,12"/>
            <TextBlock Text="Enter the details for your Arches instance. These will be saved for all future sessions." TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,16"/>

            <TextBlock Text="Instance URL" Margin="0,0,0,4"/>
            <TextBox Name="SetupInstanceURL" Margin="0,0,0,12"/>

            <TextBlock Text="Client ID" Margin="0,0,0,4"/>
            <TextBox Name="SetupClientId" Margin="0,0,0,16"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Name="SaveConfigButton" Content="Save Configuration" Height="25" Click="SaveConfig_Button"/>
                <Button Grid.Column="2" Name="CancelSetupButton" Content="Cancel" Height="25" Width="80" Visibility="Collapsed" Click="CancelSetup_Button"/>
            </Grid>

            <TextBlock Name="SetupErrorMessage" Foreground="Red" TextWrapping="Wrap" Margin="0,12,0,0" Visibility="Collapsed"/>
        </StackPanel>

        <!-- Connection panel: shown when config is valid -->
        <StackPanel Name="ConnectionPanel" Margin="20,20,20,20" Visibility="Collapsed">
            <TextBlock Text="Arches Server" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Name="InstanceURLDisplay" Text="(not configured)" Foreground="Gray" TextWrapping="Wrap" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="Change..." Padding="6,2" Margin="6,0,0,0" VerticalAlignment="Center" Click="EditConfig_Button"/>
            </Grid>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Sign In" Height="25" Click="MainSignIn_Button"/>
                <Button Grid.Column="2" Content="Sign Out" Height="25" Click="MainSignOut_Button"/>
            </Grid>

            <TextBlock Name="SigningInMessage" Text="Opening browser for sign-in..." Visibility="Hidden" FontStyle="Italic" Foreground="Gray" Margin="0,0,0,12"/>

            <Border Background="#FFF4F4F5" BorderBrush="DarkGray" BorderThickness="1" Padding="10">
                <Grid>
                    <TextBlock Name="SucceedMessage" Text="Connected to Arches Server" Visibility="Hidden" TextWrapping="Wrap"/>
                    <TextBlock Name="FailMessage" Text="Not connected" Visibility="Visible" TextWrapping="Wrap"/>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
